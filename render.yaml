databases:
  - name: x-academia-db
    plan: free

services:
  - type: web
    name: x-academia
    runtime: python
    plan: free
    buildCommand: ./build.sh
    startCommand: python -m gunicorn academia.asgi:application -k uvicorn.workers.UvicornWorker
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: x-academia-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4
